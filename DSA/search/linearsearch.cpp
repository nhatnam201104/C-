#include <iostream>
using namespace std;
int linearsearch(int a[], int n, int x)
{
    int i = 0;
    a[n] == x;
    while (a[i] != x)
    {
        i++;
    }
    if (i == n)
        return 0;   
    return 1;
}